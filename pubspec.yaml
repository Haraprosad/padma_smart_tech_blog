name: padma_smart_tech_blog
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  ###########################
  ######## UI #######
  ###########################
  cupertino_icons: ^1.0.4
  flutter_svg: ^0.21.0-nullsafety.0


  ###################################################################
  ## Navigation,Snackbars,Dialogs,Bottomsheet,State management,    ##
  ## Internationalization,Theme changing,                          ##
  ## Validators,Access data from one instance in another screen    ##
  ###################################################################
  get: ^4.6.1

  ###################################################################
  ## A fast, extra light and synchronous key-value in memory -     ##
  ## which backs up data to disk at each operation.                ##
  ###################################################################
  get_storage: ^2.0.3


  ############
  ## Logger ##
  ############
  fimber: ^0.6.4

  ###########################
  ######## networking #######
  ###########################
  dio: ^4.0.0
  pretty_dio_logger: ^1.2.0-beta-1
  connectivity: ^3.0.3


  ###########################
  ### Dependency Injection ##
  ###########################
  injectable: ^1.2.2
  get_it: ^7.2.0


  ###########################
  ###### Localizations ######
  ###########################
  flutter_localizations: # Add this line
    sdk: flutter
################################################
#For customization of splash screen easily
################################################
  splash_screen_view: ^3.0.0
  google_fonts: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  injectable_generator: ^1.2.2


  ###################################################################
  #build_runner package
  #provides a concrete way of generating files using Dart code
  ###################################################################
  build_runner:


  ##################################################################
  # To avoid string-based APIs of
  # assets, fonts, colors, flutter_gen_runner is used
  #"flutter packages pub run build_runner build" use this command
  # to generate flutter gen file
  ##################################################################
  flutter_gen_runner:


  ###############################################################
  # A command-line tool which simplifies the task of updating-
  # your Flutter app's launcher icon. Fully flexible, allowing-
  # you to choose what platform you wish to update the launcher-
  # icon for and if.
  ################################################################
  flutter_launcher_icons: ^0.9.0
flutter_icons:
  android: true
  ios: true
  image_path: "assets/img/app_icon.png"


  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^1.0.0


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/img/
    - assets/images/
    - assets/icons/
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: DANCING
      fonts:
        - asset: assets/font/dancing/DancingScript-Bold.ttf
        - asset: assets/font/dancing/DancingScript-SemiBold.ttf
        - asset: assets/font/dancing/DancingScript-Medium.ttf
        - asset: assets/font/dancing/DancingScript-Regular.ttf

    - family: SIYAM
      fonts:
        - asset: assets/font/siyam/SiyamRupaliANSI.ttf
    - family: SF-Pro-Display
      fonts:
        - asset: assets/font/SF-Pro-Display-Bold.ttf
        - asset: assets/font/SF-Pro-Display-Regular.ttf
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

###########################
#### Generate App Icon ####
###########################
# To generate flutter icon, comment out this
# code and and change Icon Path run this command

# flutter pub run flutter_launcher_icons:main

